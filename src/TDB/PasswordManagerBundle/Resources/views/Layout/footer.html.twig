<footer class="fixed-bottom bg-dark pt-3">
	<div class="container">
		<div class="row">
			<div class="col-md col-form">
				{{ form_start(form_entree, {'action': path('tdb_password_manager_addEntree'), 'attr': {'id': 'form_entree'}}) }}	
				  <div class="form-row">
				    <div class="col">
				      {{ form_widget(form_entree.entreeNom, {'attr': {
				      'class': 'form-control form-control-sm',
				      'placeholder' : 'Ajouter une Entrée'}
				  		}) }}
				    </div>
				    <div class="col col-auto">
				      {{ form_widget(form_entree.ajouter, {'attr': {
				       'class': 'btn btn-sm btn-success',
				   		}}) }}
				    </div>
				  </div>
				{{ form_end(form_entree) }}
			</div>
			<div class="col-md col-form">
				{# <form id="form-ajout-service" method="POST" action="<?= BASE_URL ?>tdb/ajoutService/" data-id="id de l'entree"> #}
				{{ form_start(form_service, {'action': path('tdb_password_manager_home'), 'attr': {'id': 'form_service', 'data-id' : ''}}) }}	
				  <div class="form-row">
				    <div class="col">
				      {# <input type="text" class="form-control form-control-sm" name="nom" placeholder="Ajouter un Service"> #}
				      {{ form_widget(form_service.serviceNom, {'attr': {
				      'class': 'form-control form-control-sm',
				      'placeholder' : 'Ajouter un Service'}
				  		}) }}
				    </div>
				    <div class="col col-auto">
				        {# <button class="btn btn-sm btn-success"  name="service" type="submit">Ajouter</button> #}
				      	{{ form_widget(form_service.ajouter, {'attr': {
				       'class': 'btn btn-sm btn-success',
				   		}}) }}
				    </div>
				    <div class="col col-auto">
					    <button class="btn btn-primary bouton-retour-haut" type="button"><i class="fas fa-arrow-up"></i></button>
				    </div>
				  </div>
				{# </form> #}
				{{ form_end(form_service) }}
			</div>
		</div>
	</div>
</footer>
